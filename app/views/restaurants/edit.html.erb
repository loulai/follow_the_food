<h1>Edit <%= @restaurant.name %></h1>

<%= render 'form' %>

<%= link_to 'Delete Restaurant', restaurant_path(@restaurant), method: :delete, data: {confirm: 'Are you sure?'} %><br>

<%= link_to 'Back to ' +  @restaurant.name, restaurant_path(@restaurant.id) %><br>
<%= link_to 'Back to Homepage', restaurants_path %>


